input:程序
output:各类错误

A.pc-lint

语言:C & CPP
编译器:VS-Studio
行业标准:MISRA
形态:本地运行
输出类型:
运行效率:并行、弹性
命令行形式、右键形式、快捷键形式、GUI按键

https://www.youtube.com/watch?v=zzkcX-0mMHU

工具内选中pc-lint链接
设置配置文件Int
项目信息
规则MISRA
单文件、多文件
生成报告

源码->配置(命令集成、Int设置、规范(MISRA)设置、源码设置)->调用(右键)->输出结果->修改代码

B.oclint

规则导入->编译->分析->报告生成
(生成AST;规则深入AST,查找不良AST节点)
(生成特定度量以更好理解源代码;基于预定义阈值,以判断是否报告为violation)
(violation输出为报告)

组成部分:

https://clang.llvm.org/docs/JSONCompilationDatabase.html
json compilation database(基于AST:编译器解析 翻译单元 需要的信息)

https://docs.oclint.org/en/stable/
directory 路径
file->数据库的键 文件
command 命令
arguments 参数
output 输出文件名

规则导入:动态链接库方式(即插即用)
规则选项:可挑选;可过滤
可保存为配置文件，用于共享

编译
设定编译选项;设置编译数据库

路径
设定项目路径

退出状态码
不同编译状态和优先级，对应不同状态码

全局分析选项
所有项目文件是否加载到内存中

Clang Static Analyzer选项
是否使用Clang Static Analyzer

Debug
是否以调试模式执行

异步设置
是否禁用异步分析

版本管理
显示版本号

报告格式

复杂性度量
https://ieeexplore.ieee.org/document/1702388

C.facebook infer
https://fbinfer.com/docs/getting-started/

infer run -- 编译器(gcc clang) -c *.c

模式:差分分析 增量式

运行前，需要清理项目 clean
捕获阶段(类似编译，生成中间结果)->
分析阶段(分析中间结果文件)(发现错误，继续分析)
(增量编译系统)

interprocedural 程序间分析
不需要程序间分析时->checker校验器(检测给定项目中每个方法的某个指定属性)

Eradicate(Java @Nullable 注解的一个检查器,消除空指针异常)


技术点:
separation logic
bi-abduction

资源泄露
(files,sockets,connections)

内存泄露
(malloc)

retain cycle(循环包含)

空指针解引用
参数非空检验
Ivar非空检验
Premature nil termination argument

D.codechecker
https://codechecker.readthedocs.io/en/v6.9.0/

CodeChecker check -b "cd ~/your-project && make clean && make" -o ~/results
CodeChecker web and storage server(localhost:8001)
SQLite database
CodeChecker store ~/results -n my-project
结果呈现:Web界面

论文:
A high-level overview about the infrastructure is available amongst the 2015Euro LLVM Conference presentations

Cross–Translation-Unit analysis

Analysis invocation 分析调用
Storage of reports to a server 保存报告到服务器
Report navigation and visualisation 记录导航和可视化
Report management, triaging tools 记录管理
Statistics, summary views 数据
Run management 运行管理
Server administration 服务器权限
(启动服务器;产品管理;认证和可获取权限)

